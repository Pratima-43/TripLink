<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <link rel="stylesheet" href="/group.css">

</head>
<body>
  <div id="img">
        <img src="groupsss.png" id="image" style="width: 1250px; height: 700px;">
    </div>

        <div id="group-buttons">
  <a href="/availablegroup">Available Groups</a>
  <a href="/joinedgroup">Joined Groups</a>
  <a href="/createdgroup">Created Groups</a>
</div>

<h2>Your Created Groups</h2>

{{#if groups.length}}
  {{#each groups}}
    <div class="group-card">
      <h3>{{groupName}}</h3>
      <p><strong>Destination:</strong> {{destination}}</p>
      <p><strong>Budget:</strong> ₹{{budget}}</p>
      <p><strong>Max Members:</strong> {{maxMembers}}</p>
      <p><strong>Transport:</strong> {{modeOfTransport}}</p>
      <p><strong>Seats Left:</strong> {{seatsLeft}}</p>

      <h4>Members:</h4>
      {{#each members}}
        <p>- {{name}} ({{email}}, {{gender}}, {{age}} yrs)</p>
      {{/each}}

      {{!-- REQUEST SECTION  --}}
      <h4>Join Requests:</h4>
    {{#if requests.length}}
      {{#each requests}}
        <p>{{name}} ({{email}}, {{gender}}, {{age}} yrs)</p>
        <a href="/accept/{{../_id}}/{{email}}"> Accept</a> |
        <a href="/decline/{{../_id}}/{{email}}">Decline</a>
      {{/each}}
    {{else}}
      <p>No pending requests</p>
    {{/if}}

      {{!-- <form action="/group/edit/{{_id}}" method="GET" style="display:inline;">
        <button type="submit">Update Group</button>
      </form> --}}
      <form action="/group/delete/{{_id}}" method="POST" style="display:inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this group?')">Delete Group</button>
      </form>
    </div>
  {{/each}}
{{else}}
  <p>No groups created by you.</p>
{{/if}}

</body>
</html>